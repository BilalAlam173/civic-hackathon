(function() {

    angular
        .module('app')
        .controller('LoginController', [
            '$mdDialog', '$interval',
            LoginController
        ]);

    function LoginController($mdDialog, $interval) {
        var vm = this;

    }

})();